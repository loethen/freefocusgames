# 项目协作规则

## 验证方式与成本

- 默认不运行 UI 测试。只有用户在当前任务中明确要求 UI 测试时，才可以执行。
- 不自行启动浏览器、运行 Playwright 或其他浏览器自动化、截图验收、执行浏览器性能测试或视觉回归。
- 页面或样式发生变化，并不构成运行 UI 测试的授权。
- 优先使用代码审查、类型检查、构建和必要的针对性非 UI 测试。HTTP 响应与生成 HTML 的检查可以按需执行。
- 检查通过后不反复运行；仅在新增修改、失败或未解决问题确有需要时重跑相关检查。控制工具输出和 token 消耗。
- 因未执行 UI 测试而存在的验证缺口，简短说明即可，不要自行补跑，也不要反复请求用户授权。

## SEO 分周执行

- 按已确认的周计划执行。每完成一周，汇报结果并等待用户确认，再进入下一周。
- 保留用户已提交的修复，先检查已有提交，再补剩余工作，避免重复实现。
- SEO 工作以技术实现为主：抓取与索引、canonical、sitemap、hreflang、页面关键词、标题描述、内容结构、内链和结构化数据。
- 不负责 GSC、GA4、AdSense 数据基线、排名观察或周期复盘，除非用户在当前任务中明确要求。
- SEO 计划、周报、审计记录和数据报告属于私有资料，保存在已被 `.gitignore` 排除的 `docs/seo/`、`docs/seo-*.md` 或 `reports/` 中，不加入公共仓库。
